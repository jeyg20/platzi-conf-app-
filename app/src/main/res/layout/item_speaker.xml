<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    app:cardCornerRadius="@dimen/cardCornerRadius"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivSpeakerImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/HeightHomeImageView"
            android:layout_margin="10dp"
            android:layout_marginVertical="@dimen/homeSubtitleMargin"
            android:src="@mipmap/ic_launcher" />
        <TextView
            android:id="@+id/tvSpeakerName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/speaker_name"
            android:textColor="@color/colorPrimaryDark"
            android:textSize="@dimen/titleSize"
            android:textStyle="bold"
            android:textAlignment="center"/>
        <TextView
            android:id="@+id/tvSpeakerWork"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/speaker_work"
            android:textColor="@color/colorAccent"
            android:textSize="@dimen/subtitleSize"
            android:textAlignment="center"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>